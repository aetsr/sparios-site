# Sparios — website

Public privacy policy, terms, support and accessibility pages for the Sparios iOS app (TR/EN/DE).
Static HTML served by GitHub Pages from `docs/`. The app's source code is not in this repository.

Edit copy in `build.py`, then run `python3 build.py`.
